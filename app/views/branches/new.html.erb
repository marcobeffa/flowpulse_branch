<% content_for :title, "New branch" %>
<% if params[:field] == "true" &&  params[:parent_id].present? %>
  <h1 class="font-bold text-4xl mbe-4">Nuovo campo </h1>
<% else %>
  <h1 class="font-bold text-4xl mbe-4">Nuovo Branch </h1>
<% end %>
<%= render "form", branch: @branch %>
<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Back to branches", branches_path, class: "btn" %>
</div>
